#include "operations.h"
/**
 * sub - find difference between two numbers and returns results
 * @a: first integer
 * @b: second integer
 *
 * Return: difference of @b from @a(on Success)
*/
signed int sub(signed int a, signed int b)
{
	return (a - b);
}
