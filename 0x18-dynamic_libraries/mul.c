#include "operations.h"
/**
 * mul - multiplies two numbers and returns results
 * @a: first integer
 * @b: second integer
 *
 * Return: Product of @a and @b(on Success)
*/
signed int mul(signed int a, signed int b)
{
	return (a * b);
}
