#include "operations.h"
/**
 * add - adds two numbers and returns results
 * @a: first integer
 * @b: second integer
 *
 * Return: Sum of @a and @b(on Success)
*/
signed int add(signed int a, signed int b)
{
	return (a + b);
}
