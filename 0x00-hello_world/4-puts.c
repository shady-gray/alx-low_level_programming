#include <stdio.h>
/**
 * main - prints a defined string to stdout
 *
 * Return: 0 (success)
*/
int main(void)
{
	const char *str;

	str = "\"Programming is like building a multilingual puzzle";
	puts(str);
	return (0);
}

